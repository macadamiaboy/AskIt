<h1 class="mb-4"><%= @question.title %></h1>

<time datetime="<%= @question.formatted_creation_time %>">
  <%= @question.formatted_creation_time %>
</time>

<p class="lead my-3">
  <%= @question.body %>
</p>

<%= link_to "Show", question_path(@question), class: 'btn btn-primary' %> |
<%= link_to "Edit", edit_question_path(@question), class: 'btn btn-secondary' %>
<div class="my-2">
  <%= button_to "Destroy", @question, method: :delete, class: 'btn btn-danger' %>
</div>

<hr class="my-3">

<h2>Write your answer</h2>

<%= render 'answers/form' %>

<h2 class="mt-5">Answers</h2>

<% @answers.each do |answer| %>
  <article class="mb-3">
    <time datetime="<% answer.formatted_creation_time %>">
      <%= answer.formatted_creation_time %>
    </time>

    <p>
      <%= answer.body %>
    </p>

    <%= link_to "Edit", edit_question_answer_path(@question, answer), class: 'btn btn-info btn-sm' %>
    <div class="my-2">
      <%= button_to "Destroy", question_answer_path(@question, answer), method: :delete, class: 'btn btn-danger btn-sm' %>
    </div>
  </article>
<% end %>